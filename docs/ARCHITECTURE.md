# 📐 项目架构文档

> English Word Game 技术架构与实现说明

## 📁 目录结构

```
English_word_game/
├── public/                   # 静态资源
├── src/
│   ├── components/           # React 组件
│   │   ├── game/            # 游戏核心组件
│   │   │   ├── GameScreen.jsx        # 主游戏界面

│   │   ├── modals/          # 弹窗组件
│   │   │   ├── ModeSelectionModal.jsx # 模式选择
│   │   │   ├── SettingsModal.jsx      # 设置弹窗
│   │   │   ├── TrophyWallModal.jsx    # 奖杯墙
│   │   │   └── WordManagerModal.jsx   # 单词管理
│   │   └── ui/              # 通用 UI 组件
│   │       ├── ErrorBoundary.jsx      # 错误边界
│   │       └── ToastNotification.jsx  # 消息提示
│   ├── constants/           # 常量定义
│   │   └── index.js         # 存储键名、记忆间隔
│   ├── data/                # 数据层
│   │   ├── achievements.js  # 成就定义

│   │   ├── units.js         # 单元元数据
│   │   └── words/           # 单词数据
│   │       ├── index.js     # 数据聚合导出
│   │       └── unit[1-6].json # 各单元单词
│   ├── styles/              # 样式文件
│   │   └── animations.css   # 动画样式
│   ├── utils/               # 工具函数
│   │   ├── audio.js         # 音频引擎
│   │   ├── helpers.js       # 通用工具
│   │   └── storage.js       # 本地存储
│   ├── App.jsx              # 应用入口
│   ├── App.css              # 全局样式
│   ├── main.jsx             # React 入口
│   └── index.css            # 基础样式
├── docs/                    # 文档目录
├── package.json
├── vite.config.js
└── tailwind.config.js
```

---

## 🧩 核心模块

### 1. 游戏引擎 (GameScreen.jsx)

主要功能：
- 字母打乱与展示
- 用户输入处理
- 正确/错误判定
- 进度跟踪

```
游戏流程:
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  加载单词   │ ──▶ │  打乱字母   │ ──▶ │  等待输入   │
└─────────────┘     └─────────────┘     └─────────────┘
                                              │
                    ┌─────────────┐           ▼
                    │  下一关     │ ◀── ┌─────────────┐
                    └─────────────┘     │  验证拼写   │
                           ▲            └─────────────┘
                           │                  │
                    ┌─────────────┐           ▼
                    │  播放音效   │ ◀── ┌─────────────┐
                    └─────────────┘     │ 正确/错误   │
                                        └─────────────┘
```

### 2. 音频引擎 (audio.js)

三级音频源降级策略：

| 优先级 | 来源 | 特点 |
|--------|------|------|
| 1 | 有道词典 API | 真人发音,延迟低 |
| 2 | Free Dictionary API | 美式发音,备用 |
| 3 | Web Speech API | 本地合成,兜底 |

```javascript
// 音频缓存机制
const audioCache = new Map();
// 首次播放后缓存URL,后续直接使用
```

### 3. 存储系统 (storage.js)

使用 localStorage 持久化以下数据：

| 键名 | 用途 |
|------|------|
| `spelling_words_v12.0` | 单词数据(含启用状态) |
| `spelling_mistakes_v12.0` | 错题本 |
| `spelling_brawl_v12.0` | 大乱斗进度 |
| `spelling_stats_v12.0` | 统计数据 |
| `spelling_achievements_v12.0` | 已解锁成就 |
| `spelling_settings_v12.0` | 用户设置 |
| `spelling_score_v12.0` | 总积分 |

### 4. 艾宾浩斯记忆算法

复习间隔设置：
```javascript
MEMORY_INTERVALS = [
  0,           // Stage 0: 立即
  5分钟,       // Stage 1
  30分钟,      // Stage 2
  12小时,      // Stage 3
  24小时,      // Stage 4
  3天,         // Stage 5
  7天          // Stage 6 (毕业)
];
```

错题处理流程：
```
错误答题 ──▶ 加入错题本 (Stage 0)
    │
    ▼
正确答题 ──▶ Stage +1, 更新下次复习时间
    │
    ▼
Stage = 6 ──▶ 从错题本毕业 🎓
```

---

## 🎨 UI 组件设计

### 状态管理

使用 React useState 管理：
- `allWordsData` - 全部单词数据
- `selectedUnit` - 当前选择的单元
- `gameMode` - 游戏模式 (visual/dictation/brawl/notebook)
- `stats` - 用户统计数据
- `unlockedAchievements` - 已解锁成就

### 响应式设计

- 移动端优先 (Mobile First)
- 使用 TailwindCSS 断点: `md:` (768px), `lg:` (1024px)
- 触控优化: `touch-manipulation`, `active:scale-95`

---

## 🔧 数据流

```
                    ┌────────────────────┐
                    │    JSON 文件       │
                    │  (unit1-6.json)    │
                    └─────────┬──────────┘
                              │ 首次加载
                              ▼
┌────────────────────────────────────────────────────┐
│                     App.jsx                         │
│  ┌─────────────────────────────────────────────┐   │
│  │            allWordsData (state)              │   │
│  │  合并: JSON基准 + localStorage用户设置       │   │
│  └─────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────┘
             │                              │
             ▼                              ▼
    ┌─────────────────┐            ┌─────────────────┐
    │   GameScreen    │            │ WordManagerModal│
    │   (游戏逻辑)    │            │   (单词管理)    │
    └─────────────────┘            └─────────────────┘
                                           │
                                           ▼
                                   ┌─────────────────┐
                                   │  localStorage   │
                                   │  (持久化保存)   │
                                   └─────────────────┘
```

---

## 📊 成就系统

### 成就类型

| 类型 | 说明 | 示例 |
|------|------|------|
| milestone | 里程碑成就 | 累计拼对100词 |
| streak | 连击成就 | 连续答对20次 |
| funny | 趣味成就 | 累计拼错100次 |
| hidden | 隐藏成就 | 点击标题10次 |

### 解锁条件检查

每次 `stats` 更新时自动检查所有成就条件。
